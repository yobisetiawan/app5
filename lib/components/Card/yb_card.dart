import 'package:flutter/material.dart';

class YbCard extends StatelessWidget {
  const YbCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}